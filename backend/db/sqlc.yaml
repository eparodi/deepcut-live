version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/store/postgres/queries/"
    schema: "db/migrations/"
    gen:
      go:
        package: "postgres"
        out: "internal/store/postgres/"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true
